# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# Expo
.expo
.expo-shared
/dist
/web-build

# Metro
.metro-cache

# TypeScript
*.tsbuildinfo

# Firebase / credentials
google-services.json
GoogleService-Info.plist

# OS / editor
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Add to .gitignore if not already there
echo "google-services.json" >> .gitignore
echo "*.jks" >> .gitignore
echo ".env" >> .gitignore

# Remove from Git index but keep local copy
git rm --cached google-services.json
git rm --cached path/to/your/keystore.jks   # if present
git rm --cached .env

# Commit the removal and the .gitignore addition
git add .gitignore
git commit -m "Ignore google-services and other secret files"
